{
  "name": "sprintask",
  "version": "1.0.0",
  "description": "Plataforma SaaS para gestión de proyectos freelance",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "build:api": "npm run build -w apps/api",
    "build:web": "npm run build -w apps/web",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web",
    "clean": "rm -rf apps/api/dist apps/web/dist node_modules",
    "db:migrate": "npm run migrate -w apps/api",
    "db:seed": "npm run seed -w apps/api",
    "db:fresh": "npm run migrate:fresh -w apps/api"
  },
  "keywords": [
    "saas",
    "project-management",
    "freelance",
    "time-tracking"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {}
}
